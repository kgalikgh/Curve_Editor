#include <SFML/Graphics.hpp>

const sf::Color nodeColor = sf::Color(102,102,102);
const sf::Color curveColor = sf::Color(30,30,30);
const sf::Color pickedNodeColor = sf::Color(200, 100, 100);
const sf::Color drawingBackgroundColor = sf::Color(200,200,200);

const int nodeSideLength = 8;

const float thickMin = 1.0;
const float thickMax = 5.0;
const float thickStep = 0.01;

const float stepMultMin = 200.0;
const float stepMultMax = 1000.0;
const float stepMultStep = 100.0;

const float divisionMin = 0.0;
const float divisionMax = 1.0;
const float divisionStep = 0.01;
